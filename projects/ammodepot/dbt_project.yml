name: 'ammodepot'
version: '1.0'

config-version: 2

profile: 'ammodepot'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  ammodepot: 
    silver:
      +schema: silver
      +materialized: view
      
    gold:
      +schema: gold
      +materialized: view
