select
    vendor_id,
    vendor_name,
    vendor_note,
    vendor_url,
    lead_time_days,
    status_id,
    account_id,
    sysuser_id,
    tax_rate_id,
    account_number,
    is_active,
    currency_id,
    credit_limit,
    date_entered,
    accounting_id,
    currency_rate,
    custom_fields,
    accounting_hash,
    minimum_order_amount,
    last_changed_user,
    date_last_modified,
    default_carrier_id,
    default_ship_terms_id,
    default_payment_terms_id,
    default_carrier_service_id
from {{ ref('fishbowl_vendors') }}
