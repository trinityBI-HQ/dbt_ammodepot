version: 2

models:
  - name: stg_bronze_test_snapshot
    description: "Stages the bronze_test_snapshot for downstream use"
    columns:
      - name: id
        tests: [not_null, unique]

      - name: user_id
        tests: [not_null]

      - name: amount
        tests:
          - not_null

      - name: order_ts
        tests:
          - not_null
