{{ config(materialized='view') }}

SELECT 
    EMAIL,
    RP_TOKEN,
    DEFAULT_BILLING,
    TAXVAT,
    PREFIX,
    PASSWORD_HASH,
    CREATED_IN,
    STORE_ID,
    DOB,
    IS_ZENDESK_USER,
    UPDATED_AT,
    SUFFIX,
    SESSION_CUTOFF,
    LOCK_EXPIRES,
    DISABLE_AUTO_GROUP_CHANGE,
    CONFIRMATION,
    INCREMENT_ID,
    FIRSTNAME,
    WEBSITE_ID,
    FAILURES_NUM,
    LASTNAME,
    GENDER,
    IS_ACTIVE,
    ENTITY_ID,
    CREATED_AT,
    MIDDLENAME,
    GROUP_ID,
    MP_SMTP_EMAIL_MARKETING_SYNCED,
    DEFAULT_SHIPPING,
    FIRST_FAILURE,
    RP_TOKEN_CREATED_AT
FROM {{ source('nome_do_schema', 'customer_entity') }}
